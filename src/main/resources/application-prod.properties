# Production profile — activated automatically on Replit via SPRING_PROFILES_ACTIVE=prod
# Set these as Replit Secrets (Environment Variables):
#   EUREKA_HOSTNAME  → your Replit app URL e.g. eureka-server.yourusername.repl.co
#   EUREKA_PASSWORD  → a strong password for HTTP Basic auth

server.port=${PORT:8761}

eureka.instance.hostname=${EUREKA_HOSTNAME}
eureka.instance.prefer-ip-address=false
eureka.instance.non-secure-port=80
eureka.instance.non-secure-port-enabled=true

eureka.client.register-with-eureka=false
eureka.client.fetch-registry=false
eureka.client.service-url.defaultZone=http://admin:${EUREKA_PASSWORD}@${EUREKA_HOSTNAME}/eureka/

eureka.server.enable-self-preservation=true
eureka.server.eviction-interval-timer-in-ms=30000

spring.security.user.name=admin
spring.security.user.password=${EUREKA_PASSWORD}

logging.level.com.netflix.eureka=WARN
logging.level.com.netflix.discovery=WARN
