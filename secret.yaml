apiVersion: v1
kind: Secret
metadata:
  name: api-gateway-secrets
  labels:
    app: api-gateway
    project: codesync
type: Opaque
stringData:
  # ── REQUIRED: Replace these values before applying ───────────────────────
  #
  # JWT_SECRET: Must be a Base64-encoded string, minimum 32 bytes decoded.
  # Generate one with:  openssl rand -base64 48
  JWT_SECRET: "KdS0PtJXQHBbGpnI0h/evLtbDNDaauEx6EAL8qnc1j8="

  # EUREKA_PASSWORD: Must match the password set in your Eureka Server's Secrets.
  EUREKA_PASSWORD: "admin123"
